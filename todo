.tmp save files in the close function so i can open where was left off.

offscreen multiline comments broken

sorted by importance:


comments for other languages // not only
filebroser max hardcoded 100, so allocation of too many files breaks it.

scrollbar.

32 bit version bug
tab autocomplete directives

highlighting for more languages than C, and general lua (function is a keyword)
code folding
check for file change outside editor and reload on window focus event
find/replace
swap files
